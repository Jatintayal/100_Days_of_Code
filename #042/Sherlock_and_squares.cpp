#include<iostream>
#include<cmath>
using namespace std;

int main()
{
	long long t,a,b;
	cin>>t;
	while(t--)
	{
			long long r=(long long)sqrt(b)-(long long)sqrt(a);
			
			if(sqrt(a) == (long)sqrt(a))
				r++;
			cout<<r<<endl;
	}
	return 0;
}
